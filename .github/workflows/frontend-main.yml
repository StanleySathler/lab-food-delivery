name: "Main: Frontend QA"

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install npm modules
        shell: bash
        run: |
          cd frontend
          npm ci

      - name: Run tests
        shell: bash
        run: |
          cd frontend
          npm run test:unit
