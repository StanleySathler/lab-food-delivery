## Development

**1. Install packages**

```bash
npm ci
```

> `npm ci`, unlikely `npm install`, installs exact same versions as specified in `package-lock.json`. This ensures we have the exact same versions across all environments.

**2. Run server**

```bash
npm run start:dev
```
